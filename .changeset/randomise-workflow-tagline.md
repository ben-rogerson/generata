---
"@generata/core": patch
---

Randomise the workflow start tagline. Replaces the static "Let's get to work." banner with a randomly picked tagline so consecutive runs feel a bit livelier, matching the existing per-agent tagline behaviour.
