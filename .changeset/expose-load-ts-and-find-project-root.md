---
"@generata/core": minor
---

Expose `loadTs` and `findProjectRoot` as public exports so consumer packages (notably the upcoming `@generata/serve`) can resolve project-relative TypeScript modules.
