---
"@generata/core": minor
---

Brand `WorktreeConfig` so `isolation` requires the `worktree()` helper. The previous structural type let raw object literals assign through; the brand forces consumers to use the helper or omit the field.
