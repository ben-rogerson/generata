---
"@generata/core": minor
---

Remove agent summaries feature. The post-agent humanize step added latency and cost to every run for a recap most users skipped, and the `agentSummaries` config option is now gone.
