---
"@generata/core": patch
---

Print help for bare `generata` invocation instead of `Unknown command: undefined`. New users running the CLI with no arguments now discover the available subcommands.
